---
import { renderMarkdown } from "@/utils/markdown";
import HtmlContent from "../HtmlContent/HtmlContent.astro";

type Props = {
  content: string;
};

const html = await renderMarkdown(Astro.props.content);
---

<HtmlContent set:html={html} />
