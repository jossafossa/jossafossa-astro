---
import Clickable from "../Clickable/Clickable.astro";
import styles from "./Button.module.scss";

const { class: className, ...rest } = Astro.props;
---

<Clickable {...rest} class:list={[styles.button, className]}>
  <slot />
</Clickable>
