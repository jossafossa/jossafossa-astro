---
import styles from "./Paragraph.module.scss";

type Props = {
  variant?: "normal" | "muted";
  size?: "small" | "default" | "large";
};

const { variant = "normal", size = "default" } = Astro.props;

---

<p class:list={[styles.paragraph, styles[variant], styles[size]]}>
  <slot />
</p>
