---
interface Props {
  type?: 'info' | 'success' | 'warning' | 'danger';
  title?: string;
}

const { type = 'info', title } = Astro.props;
---

<div>
  {title && <strong>{title}</strong>}
  <div>
    <slot />
  </div>
</div>

